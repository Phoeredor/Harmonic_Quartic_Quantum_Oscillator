# Runtime data

Raw and processed simulation outputs are intentionally not distributed. Local
simulation and analysis workflows write their runtime outputs below this
directory, where generated data remain ignored by Git.

The report figures distributed with the repository are available under
`../plots/`. Reproducing the numerical results requires running the simulations
and analyses locally to produce the required data first.
